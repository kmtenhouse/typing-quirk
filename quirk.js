const Substitution = require("./substitution");
const regexgen = require('regexgen');

class Quirk {
    constructor(name) {
        //create the plain version of the 
        this.name = name;
    }
}

module.exports = Quirk;